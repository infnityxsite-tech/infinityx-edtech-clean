# Production Environment Variables for Vercel Deployment

# Database Configuration (Use Neon PostgreSQL or PlanetScale MySQL)
# For Neon (PostgreSQL): Get from https://neon.tech (Free tier available)
# For PlanetScale (MySQL): Get from https://planetscale.com (Free tier available)
DATABASE_URL="mysql://username:password@host/database?sslaccept=strict"

# Session Secret (Generate a random string for production)
# You can generate one with: openssl rand -base64 32
SESSION_SECRET="your-production-secret-key-change-this"

# Node Environment
NODE_ENV="production"

# Optional: If you want to use a different port
PORT=3000
